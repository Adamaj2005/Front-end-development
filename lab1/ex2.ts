//addition function to output value 
function addition(value: string) {
    console.log("Value is: " + value);
    }
    let firstVal: number = 42;  // variables being declared using number and string
    let secondVal: number = 1;
    let sumOfVals: string = (firstVal + secondVal).toLocaleString();
    addition(sumOfVals); //calling function and adding first val and second val 
