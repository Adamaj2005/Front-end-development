//addition function to output value 
function addition(value) {
    console.log("Value is: " + value);
}
var firstVal = 42; // variables being declared using number and string
var secondVal = 1;
var sumOfVals = (firstVal + secondVal).toLocaleString();
addition(sumOfVals); //calling function and adding first val and second val 
